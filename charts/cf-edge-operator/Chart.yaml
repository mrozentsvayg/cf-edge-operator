apiVersion: v2
name: cf-edge-operator
description: Kubernetes operator for Cloudflare edge features (custom hostnames, Origin SNI, SSL lifecycle, drift detection, load balancing)
type: application
# version: chart version, bumped independently for chart-only changes (values, templates, CRDs).
# appVersion: default image tag (without v prefix). Bumped on operator code changes.
version: 0.4.1
appVersion: "0.5.0"
